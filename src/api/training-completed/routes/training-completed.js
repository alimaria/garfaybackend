'use strict';

/**
 * training-completed router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::training-completed.training-completed');
